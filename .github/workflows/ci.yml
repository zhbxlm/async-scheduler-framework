name: CI

on:
  push:
    branches: [ main, feat/*, fix/* ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']

    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      mysql:
        image: mysql:8
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: testpass
          MYSQL_DATABASE: scheduler_test
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[dev]
        pip install pytest pytest-asyncio pytest-timeout httpx
    
    - name: Wait for Redis
      run: |
        for i in {1..30}; do
          if redis-cli -h localhost ping | grep -q PONG; then
            echo "Redis is ready"
            break
          fi
          echo "Waiting for Redis..."
          sleep 1
        done
    
    - name: Wait for MySQL
      run: |
        for i in {1..30}; do
          if mysqladmin -h localhost -u root -ptestpass ping 2>/dev/null | grep -q 'mysqld is alive'; then
            echo "MySQL is ready"
            break
          fi
          echo "Waiting for MySQL..."
          sleep 1
        done
    
    - name: Run tests
      env:
        REDIS_URL: redis://localhost:6379/0
        MYSQL_URL: mysql://root:testpass@localhost/scheduler_test
      run: |
        pip install pytest-cov
        python -m pytest tests/ --tb=short -v --cov=src --cov-report=xml --cov-report=term
    
    - name: Test CLI
      run: |
        python -m pip install click httpx
        python -c "from src.cli.main import cli; print('CLI imports successfully')"
    
    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 src/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 src/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: Type check with mypy (optional)
      run: |
        pip install mypy
        mypy src/ --ignore-missing-imports || echo "Type checking completed (warnings allowed)"

    - name: Security scanning with bandit
      run: |
        pip install bandit
        bandit -r src/ -f json -o bandit-report.json || echo "Bandit scan completed"

    - name: Dependency vulnerability scanning
      run: |
        pip install safety
        safety check --json --output safety-report.json || echo "Safety check completed"

  build-packages:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Build packages
      run: |
        cd packages/node-agent && pip install build && python -m build
        cd ../async-proxy && pip install build && python -m build
        cd ../worker-sdk && pip install build && python -m build
    
    - name: Build documentation
      run: |
        pip install mkdocs mkdocs-material mkdocstrings-python
        mkdocs build --site-dir _site
    
    - name: Upload package artifacts
      uses: actions/upload-artifact@v4
      with:
        name: python-packages
        path: |
          packages/*/dist/*.whl
          packages/*/dist/*.tar.gz
    
    - name: Upload documentation
      uses: actions/upload-artifact@v4
      with:
        name: documentation
        path: _site/