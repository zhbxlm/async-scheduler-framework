# Package entry layer for scheduler-control-plane.
# Source of truth lives in src/{common,models,platform,services}/.
# This package is built into a standalone wheel via scripts/bundle_package.py.
"""Public package entry for async-scheduler-control-plane."""
