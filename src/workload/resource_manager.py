"""ResourceManager — node allocation + autoscaling.

This is a placeholder — the real ResourceManager is in src/platform/resource_manager.py
"""
print("Note: ResourceManager is already implemented at src/platform/resource_manager.py")
