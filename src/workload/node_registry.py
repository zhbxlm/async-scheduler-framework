"""NodeRegistry — Redis-backed node state registry.

This is a placeholder — the real NodeRegistry is in src/platform/node_registry.py
"""
print("Note: NodeRegistry is already implemented at src/platform/node_registry.py")
